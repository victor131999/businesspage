---
import CardsCard from "./cards-components/CardsCard";
interface Props {
  noInteraction?: boolean;
  isDemoEnabled?: boolean;
}

const { noInteraction = true, isDemoEnabled = true } = Astro.props;
---

<div class={noInteraction ? "no-interaction" : ""}>
  <CardsCard client:visible isDemoEnabled={isDemoEnabled} />
</div>

<style>
  .no-interaction {
    width: 100%;
    height: 100%;
    pointer-events: none !important;
    user-select: none !important;
  }
  :global(.cards-card-root) {
    width: 100%;
    height: 100%;
  }
</style>
