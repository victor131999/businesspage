---
import Navbar from "../components/navbar.astro";
import ProductsPanel from "../components/productsPanel.astro";
import "../styles/navbar.css";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Zelify Page 3 — AML</title>
  </head>

  <body
    style="
      background-color: #0e1510;
      background-image: url('/images/FONDO 4.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      min-height: 100vh;
      margin: 0;
    "
  >
    <Navbar
      logoSrc="/images/zelify_logo.png"
      colors={{
        text: "#054F8B",
        textHover: "#ffffff",
        highlightBg: "#00223E",
        highlightText: "#6AFF00",
        btnLightBg: "#E7EDF6",
        btnLightHoverBg: "#DAE4F1",
        btnLightText: "#004195",
        btnOutline: "#00223E",
        btnOutlineHover: "#ffffff",
        dropdownBg: "#ffffff",
        dropdownLink: "#1e2b3a",
        dropdownHoverBg: "#EEF3F9",
        radius: "22px",
      }}
      buttons={{
        first: { label: "Work with us", href: "https://www.zelify.com/jobs" },
        second: { label: "Business access", href: "https://www.zelify.io/" },
        third: { label: "EN", href: "/" },
      }}
    />
<ProductsPanel />
    <section class="hero">
      <div class="content">
        <span class="band" data-i18n="aml.badge">AML</span>

        <h1 class="title" data-i18n="aml.title" data-i18n-html="true">
          Smart Risk.<br />
          Zero Blind Spots.
        </h1>

        <p class="description" data-i18n="aml.description" data-i18n-html="true">
          Automated PEP, sanctions, and media screening built to expose financial risk before it
          strikes. Stay compliant, fight crime, and keep every transaction under control.
        </p>

        <div class="cta">
          <a href="/hablemos" class="btn" data-i18n="aml.ctaPrimary">Let's talk</a>
          <a href="https://documentations.zelify.com/servicios-financieros/zelify-aml" target="_blank" rel="noopener noreferrer" class="btn" data-i18n="aml.ctaSecondary">View API Docs</a>
        </div>
      </div>
    </section>
    <script is:inline>
      (() => {
        const translations = {
          EN: {
            "aml.badge": "AML",
            "aml.title": "Smart Risk.<br />Zero Blind Spots.",
            "aml.description":
              "Automated PEP, sanctions, and media screening built to expose financial risk before it strikes.<br />Stay compliant, fight crime, and keep every transaction under control.",
            "aml.ctaPrimary": "Let’s talk",
            "aml.ctaSecondary": "View API Docs",
          },
          ES: {
            "aml.badge": "AML",
            "aml.title": "Riesgo inteligente.<br />Sin puntos ciegos.",
            "aml.description":
              "Screening automatizado de PEP, sanciones y medios que revela el riesgo financiero antes de que ocurra.<br />Mantente en cumplimiento, combate el fraude y mantiene cada transacción bajo control.",
            "aml.ctaPrimary": "Hablemos",
            "aml.ctaSecondary": "Ver documentación API",
          },
        };

        const applyTranslations = (lang) => {
          const dictionary = translations[lang] ?? translations.EN;
          document.querySelectorAll("[data-i18n]").forEach((node) => {
            const key = node.dataset.i18n;
            if (!key) return;
            const value = dictionary[key];
            if (!value) return;
            if (node.dataset.i18nHtml === "true") {
              node.innerHTML = value;
            } else {
              node.textContent = value;
            }
          });
        };

        const initialLang =
          window.__uiLanguage ??
          (document.documentElement.lang ? document.documentElement.lang.toUpperCase() : "EN");
        applyTranslations(initialLang);

        window.addEventListener("ui:languagechange", (event) => {
          applyTranslations(event.detail?.language === "ES" ? "ES" : "EN");
        });
      })();
    </script>
  </body>
</html>

<style>
  @font-face {
    font-family: "NataSans";
    src: url("/fonts/NataSans-VariableFont_wght.ttf") format("truetype");
    font-weight: 100 900;
    font-display: swap;
  }

  body {
    font-family: "NataSans", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    color: #0b132b;
  }

  .hero {
    min-height: calc(100vh - 80px);
    display: flex;
    align-items: center;
    padding: 4rem 8% 6rem;
  }

  .content {
    max-width: 640px;
    text-align: left;
    margin-left: 200px;    
    margin-right: 6%;     
  }

  .band {
    display: inline-block;
    background: #183b86;
    color: #e9eef7;
    padding: 0.85rem 2rem;
    border-radius: 40px;
    font-weight: 800;
    letter-spacing: 0.3px;
    margin-bottom: 2rem;
    max-width: 350px;
  }

  .title {
    color: #0b132b;
    font-weight: 800;
    line-height: 1.1;
    font-size: clamp(2.2rem, 5vw, 3.2rem);
    margin: 0 0 1rem 0;
  }

  .description {
    color: #223547;
    font-size: 1.05rem;
    line-height: 1.65;
    max-width: 520px;
    margin: 0 0 1.8rem 0;
  }

  .cta {
    display: flex;
    gap: 0.9rem;
    flex-wrap: wrap;
  }

  .cta .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    background: linear-gradient(145deg, rgba(128, 255, 0, 0.94), rgba(88, 220, 0, 0.88));
    color: #0d2208;
    text-decoration: none;
    font-weight: 600;
    padding: 0.55rem 1.65rem;
    border-radius: 16px;
    font-size: 0.95rem;
    letter-spacing: 0.02em;
    box-shadow: 0 12px 26px rgba(80, 220, 0, 0.32);
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }

  .cta .btn::before {
    content: "";
    width: 18px;
    height: 18px;
    background: url("/icons/maletin.svg") no-repeat center / contain;
    filter: drop-shadow(0 2px 2px rgba(0, 0, 0, 0.2));
    display: inline-block;
  }

  .cta .btn + .btn::before {
    background: url("/icons/acceso.svg") no-repeat center / contain;
  }

  .cta .btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 16px 30px rgba(80, 220, 0, 0.36);
  }

  @media (max-width: 960px) {
    .hero { padding: 4rem 6%; }
    .content {
      max-width: 100%;
      margin: 0 auto;
      text-align: center;
    }
    .title { font-size: clamp(2rem, 6vw, 3rem); }
    .band { max-width: 100%; text-align: center; }
  }
</style>
